import { styled } from "styled-components";


export const StyledHeader = styled.div`
    color: black;
    margin: 75px;
    margin-bottom: 36px;

    h1, p {
        margin: 0;
    }

    p {
       margin-top: 10px;
       color: darkgray;
    }
`